{
  "suite": "bootstrap",
  "environment": {
    "excel_version": "hand-seeded",
    "excel_locale": "ja-JP",
    "date1904": false,
    "iterative": false,
    "generated_at": "2026-04-23",
    "note": "These 5 cases are not generated by the oracle-gen pipeline. They are hand-seeded to keep the oracle-verify test target exercising the plumbing before a Mac has run the real generator. Every case here is trivially correct and should match Excel byte-for-byte."
  },
  "tolerance": {"abs": 0.0, "rel": 0.0},
  "cases": [
    {
      "id": "literal_add",
      "formula": "=1+1",
      "setup": {},
      "expect": {"kind": "number", "value": 2.0}
    },
    {
      "id": "sum_literals",
      "formula": "=SUM(1,2,3)",
      "setup": {},
      "expect": {"kind": "number", "value": 6.0}
    },
    {
      "id": "if_true_text_branch",
      "formula": "=IF(TRUE,\"a\",\"b\")",
      "setup": {},
      "expect": {"kind": "text", "value": "a"}
    },
    {
      "id": "div_by_zero",
      "formula": "=2/0",
      "setup": {},
      "expect": {"kind": "error", "code": "#DIV/0!"}
    },
    {
      "id": "count_mixed_range",
      "formula": "=COUNT(A1:A3)",
      "setup": {
        "A1": {"kind": "number", "value": 1},
        "A2": {"kind": "text", "value": "hello"},
        "A3": {"kind": "number", "value": 3}
      },
      "expect": {"kind": "number", "value": 2.0}
    }
  ]
}
