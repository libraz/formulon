{
  "suite": "mode_mult",
  "description": "MODE.MULT vertical-array mode spill — five perspectives spanning two-way tie (2x1 spill), single-mode 1x1 spill, all-unique input ->",
  "environment": {
    "excel_version": "16.0",
    "excel_locale": "ja-JP",
    "date1904": false,
    "iterative": false,
    "generated_at": "2026-05-14T23:44:10Z"
  },
  "tolerance": {
    "abs": 1e-12,
    "rel": 1e-12
  },
  "cases": [
    {
      "id": "mode_mult_two_way_tie",
      "formula": "=MODE.MULT(1,2,1,2,3)",
      "setup": {},
      "skipped": "MODE.MULT returns a 2x1 ArrayValue; oracle compare_value only handles scalars, INDEX wrappers blocked by resolve_range_arg. Unit tests validate vertical-spill content directly."
    },
    {
      "id": "mode_mult_single_mode_1x1",
      "formula": "=MODE.MULT({1,2,1})",
      "setup": {},
      "skipped": "MODE.MULT with a single mode returns a 1x1 ArrayValue (still vertical-array shape); oracle compare_value only handles scalars. Unit tests validate the boundary."
    },
    {
      "id": "mode_mult_all_unique_na",
      "formula": "=MODE.MULT(1,2,3,4)",
      "setup": {},
      "expect": {
        "kind": "error",
        "code": "#N/A"
      }
    },
    {
      "id": "mode_mult_singleton_na",
      "formula": "=MODE.MULT(5)",
      "setup": {},
      "expect": {
        "kind": "error",
        "code": "#N/A"
      }
    },
    {
      "id": "mode_mult_error_propagates",
      "formula": "=MODE.MULT(1, #DIV/0!, 2)",
      "setup": {},
      "expect": {
        "kind": "error",
        "code": "#DIV/0!"
      }
    }
  ]
}
